# CODEOWNERS — fs-packages (The Armory)
#
# fs-packages is the OIDC Trusted-Publishing root for every @script-development/*
# package consumed across the fleet. Every change requires review from a repo
# admin — the CI/release surface (.github/**) is the highest-leverage supply-chain
# path here, so the same owner set guards the whole repo.
#
# Routes to the @script-development/fs-packages-admins org team, mirroring
# phpstan-warroom-rules' single-team CODEOWNERS shape. The team is the dedicated
# Armory-admin set (a deliberately tight roster, like its phpstan sibling) rather
# than the full repo-admin collaborator list. INVARIANT: the team MUST retain
# write (push) access to this repo — a CODEOWNERS team without repo access is
# silently ignored by GitHub, which would delete owner-review enforcement on the
# supply-chain root with zero signal.

* @script-development/fs-packages-admins
